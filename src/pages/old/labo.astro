---
// Tu peux modifier le chemin vers ton image bannière ici
const imageBanniere = "/images/baniere_test.png" ;
---


<html lang="fr">
  <head>
    <meta charset="UTF-8" />
    <title>Atelier de réparation d'amplis</title>

    <style>
      /* Corps de la page : police, centrage du texte */
      body {
        margin: 0;
        font-family: sans-serif;
        text-align: center;

      }

      /* Banniere en haut : image pleine largeur */
      header {
      background-image: url('/images/baniere_test.png');
  		background-size: 50%;         /* Redimensionne pour couvrir tout le bloc */
  	background-position: center;    /* Centrage horizontal + vertical */
  	background-repeat: no-repeat;
  	height: 180px;    
      }

      /* Menu de navigation */
      nav {
        margin: 20px 0;
      }

      /* Style des liens de navigation */
      nav a {
        margin: 0 15px;
        text-decoration: none;
        color: black;
        font-weight: bold;
      }

      /* Titre principal */
      h1 {
        font-size: 2em;
        margin-top: 40px;
      }

      /* Titres de sections */
      h2 {
        margin-top: 40px;
        font-size: 1.5em;
      }

      /* Encadré du formulaire */
      .contact {
        margin: 40px auto;
        max-width: 500px;
        text-align: left;
        background: #f7f7f7;
        padding: 20px;
        border-radius: 8px;
      }

      /* Champs du formulaire */
      input, textarea {
        width: 100%;
        margin-top: 10px;
        margin-bottom: 20px;
        padding: 10px;
        border: 1px solid #ccc;
        border-radius: 4px;
        font-family: inherit;
      }

      /* Bouton du formulaire */
      button {
        padding: 10px 20px;
        background-color: black;
        color: white;
        border: none;
        border-radius: 4px;
        cursor: pointer;
      }

      /* Section photos */
      .photos {
        margin: 50px auto;
      }

      .photos img {
        max-width: 300px;
        margin: 10px;
      }

	  .container {
  max-width: 50%;
  margin: 0 auto; /* centre horizontalement */
  text-align: left; /* si tu préfères le texte aligné à gauche */
}
	  
    </style>
  </head>

  <body>
    <!-- En-tête avec l'image bannière -->
    <header></header>

    <!-- Menu de navigation vers les autres pages -->
    <nav>
      <a href="/">Réparation</a>         <!-- Page actuelle -->
      <a href="/amplis">Amplis</a>         <!-- Page à créer -->
      <a href="/labo">Labo</a>           <!-- Page à créer -->
      <a href="/a-propos">À propos</a>   <!-- Page à créer -->
      <a href="/contact">Contact</a>     <!-- Page à créer -->
    </nav>

    <!-- Contenu principal centré -->
    <main>
		<div class="container">
      <h1>Le Labo</h1>
      <p><strong>Blog technique</strong></p>

      <p>Vous trouverez dans cette section du site que je mettrais à jour régulièrement, une bibliographie de différents projets chinés en ligne particulièrement utiles. </p>
		</div>


      <!-- Galerie de photos (à remplir avec tes propres images) -->
      <div class="photos">
        <img src="/images/champ_flower_test.jpeg" alt="Flower Champ" />
        <!-- Tu peux ajouter d'autres images ici -->
      </div>
    </main>
  </body>
</html>